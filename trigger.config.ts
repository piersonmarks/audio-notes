import { defineConfig } from "@trigger.dev/sdk";

export default defineConfig({
  project: "proj_dfaxfiuxalblwgxhrlea",
  dirs: ["./src/trigger"],
  maxDuration: 300, // 5 minutes max for audio transcription tasks
});
